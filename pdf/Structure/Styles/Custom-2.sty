%%%%%%%%%%%%%% COLOURS
%%%%%%%%%%%%%% TO PRINT IN BLACK AND WHITE, CHANGE ALL COLOURS TO '0,0,0'
\definecolor{Url}{RGB}{238,34,0}
\definecolor{Cite}{RGB}{46,112,64}
\definecolor{Link}{RGB}{4,72,124}
\definecolor{MarginNote}{RGB}{228,84,54}

%%%%%%%%%%%%%%      FONTS
%\defaultfontfeatures{Ligatures=NoCommon, RawFeature={-liga,-dlig,-clig,-hlig,-calt}}
\setmainfont{Aptos Serif}%[RawFeature={-liga,-dlig,-clig,-hlig,-calt}]
\setsansfont{Aptos}%[RawFeature={-liga,-dlig,-clig,-hlig,-calt}]
\setmonofont{JetBrainsMono Nerd Font Mono}[RawFeature={-liga,-dlig,-clig,-hlig,-calt}]

%%%%%%%%%%%%%%      PAGE FORMAT
\geometry{
  a4paper,
  twoside=false,       % identical left/right pages (no mirroring)
  left=2cm,
  right=2cm,
  top=2.5cm,
  bottom=2.5cm,
  includefoot,         % include footer area in the total page layout
  voffset=0pt,
  hoffset=-2pt,
  headheight=54pt,
  headsep=\baselineskip,
  footskip=15pt,       % smaller gap between text and footer
  footnotesep=10pt
  %showframe           % uncomment to visualize margins
}

%%%%%%%%%%%%%	FOOTNOTES
\makeatletter
\renewcommand\footnoterule{%
  \vskip 10pt % Space above the line
  \hrule width 0.4\columnwidth height 0.4pt
  \kern 5pt  % Space below the line
}
\makeatother

%%%%%%%%%%      FIGURE CAPTIONS
\usepackage{caption}
\captionsetup{
	format=hang,
	indention=0cm,
	labelformat= simple,		%%%%%    or 'empty'
	labelsep=period,		    %%%%%    or 'none', 'colon', 'space'
	labelfont=bf,
	width=.6\textwidth,
	skip=15pt
}
